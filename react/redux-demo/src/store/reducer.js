const defaultState = {
    inputValue: 'Write Something',
    list: [
        '早8点开晨会，分配今天任务',
        '早9点开需求沟通会',
        '早9点开需求沟通会'
    ]
}

export default (state = defaultState, action) => {
    return state
}